{
  "name": "college-erp",
  "private": true,
  "version": "0.1.0",
  "description": "Production-focused College ERP foundation with contextual permissions.",
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend \"npm run dev -w apps/backend\" \"npm run dev -w apps/frontend\"",
    "build": "npm run build -w apps/backend && npm run build -w apps/frontend",
    "typecheck": "npm run typecheck -w apps/backend && npm run typecheck -w apps/frontend",
    "test": "npm run test -w apps/backend",
    "lint": "npm run lint -w apps/backend && npm run lint -w apps/frontend",
    "check": "npm run prisma:generate && npm run typecheck && npm run lint && npm run test && npm run build",
    "smoke": "node scripts/smoke.mjs",
    "prisma:generate": "npm run prisma:generate -w apps/backend",
    "prisma:migrate": "npm run prisma:migrate -w apps/backend"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
